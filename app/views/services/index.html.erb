<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">

      <h1 class="text-center">Lista de los conserjes</h1>

          <% @services.each do |service| %>
          <%= link_to service_path(service) do %>
      //avatar du créateur de service
      <%= cl_image_tag seller.avatar.path, height: 50, crop: :fill %>
<div class="product">



  <div class='product-body'>
    <h3><%= service.name %></h3>
    <p><%= service.city %></p>
  </div>


</div>

      <% end %>
      <% end %>
      <br>
      <%= link_to 'Crea tu anuncio de conserje', new_service_path, class: "btn btn-success col-sm-offset-5" %>
    </div>

  </div>
