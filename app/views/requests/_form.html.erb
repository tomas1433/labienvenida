<%= simple_form_for([@service, @request]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :date %>

  </div>

  <div class="form-actions">
    <%= f.button :submit, "Finalizar solicitud", class: "btn btn-success" %>
  </div>
<% end %>
